## TODO

- [ ] Criar uma GitHub Action para executar o deploy no Cloudflare Workers quando uma nova tag (`v*`) for criada.
- [ ] Configurar os secrets `CLOUDFLARE_API_TOKEN` e `CLOUDFLARE_ACCOUNT_ID`.
- [ ] Executar `npm ci`.
- [ ] Executar `npm run build`.
- [ ] Executar `npx wrangler deploy`.
